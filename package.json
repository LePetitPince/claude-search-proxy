{
  "name": "claude-search-proxy",
  "version": "1.0.0",
  "description": "OpenAI-compatible HTTP proxy that turns Claude CLI's built-in WebSearch into a search API",
  "type": "module",
  "bin": {
    "claude-search-proxy": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "tsc -p tsconfig.test.json && node --test dist/test/*.test.js",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "license": "MIT",
  "keywords": ["claude", "search", "proxy", "openai", "api", "websearch"],
  "repository": {
    "type": "git", 
    "url": "https://github.com/LePetitPince/claude-search-proxy.git"
  },
  "author": "LePetitPince"
}